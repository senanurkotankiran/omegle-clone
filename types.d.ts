export interface JwtToken {
    id: string; // id özelliğini tanımlayın
    // Diğer özellikler varsa burada tanımlayın
  }interface UserSession {
    id: string; // Define the id property here
    // Other properties if needed
  }